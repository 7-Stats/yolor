#!/usr/bin/env python
from setuptools import find_packages, setup

install_requires=[  'argparse',
                    'pandas',
                    'numpy',
                    'bs4',
                    'requests']

setup(name='yolor')
